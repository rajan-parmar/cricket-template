name: 'Check for master'
on: 
  pull_request:
    branches:
      - master
      - main
jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v3
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
   
      
    
      
        
